export const metadata = {
    title:'Login',
    description: 'CLub Login Page'
}


export default function loginLayout({ children }) {
    return (
      <>
          {children}
          </>
          
    )
  }
  