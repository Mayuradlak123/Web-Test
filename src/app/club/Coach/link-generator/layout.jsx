export const metadata = {
    title:'Link Generator',
    description: 'Generate a Link to give it to a Client'
}


export default function linkGeneratorLayout({ children }) {
    return (
      <>
          {children}
          </>
          
    )
  }
  